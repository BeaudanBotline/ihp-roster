Do not work manually.

From the repo root, run exactly:

bash ./.loom/bin/codex-workstream-loop.sh \
--workstream roster-live-fragments \
--iterations 2 \
--model gpt-5.4

After it exits, report whether both iterations completed.
EOF
